import 'package:flutter/material.dart';

const textTitleStyle = TextStyle(
  color: Colors.white,
  fontWeight: FontWeight.bold,
  shadows: [Shadow(color: Colors.black, offset: Offset(1, 0), blurRadius: 8)],
);
